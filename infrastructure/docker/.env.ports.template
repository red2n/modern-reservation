# Environment variables for secure Docker deployment
# Copy this file to .env.ports and customize as needed

# ============================================
# DATABASE
# ============================================
POSTGRES_DB=modern_reservation
POSTGRES_USER=mruser
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_PORT=5432

# ============================================
# SECURITY
# ============================================
JWT_SECRET=change-me-in-production-use-strong-secret
JWT_EXPIRES_IN=1h

# ============================================
# INFRASTRUCTURE PORTS (Internal Only)
# ============================================
CONFIG_SERVER_PORT=8888
EUREKA_SERVER_PORT=8761
REDIS_PORT=6379
KAFKA_PORT=9092
ZOOKEEPER_PORT=2181

# ============================================
# GATEWAY (External Access)
# ============================================
GATEWAY_PORT=8080

# ============================================
# FRONTEND (External Access)
# ============================================
GUEST_PORTAL_PORT=3000
ADMIN_PORTAL_PORT=3001

# ============================================
# NODE.JS SERVICES (Internal Only)
# ============================================
AUTH_SERVICE_PORT=3100
NOTIFICATION_SERVICE_PORT=3101
WEBSOCKET_SERVICE_PORT=3102

# ============================================
# JAVA BUSINESS SERVICES (Internal Only)
# ============================================
PROPERTY_SERVICE_PORT=8101
RESERVATION_ENGINE_PORT=8102
RATE_SERVICE_PORT=8103
PAYMENT_SERVICE_PORT=8104
GUEST_SERVICE_PORT=8105
REPORTING_SERVICE_PORT=8106
NOTIFICATION_PROCESSOR_PORT=8107

# ============================================
# API ENDPOINTS
# ============================================
NEXT_PUBLIC_API_URL=http://localhost:8080/api
NEXT_PUBLIC_WS_URL=ws://localhost:8080/ws

# ============================================
# MONITORING (Internal Only)
# ============================================
GRAFANA_PORT=3003
PROMETHEUS_PORT=9090
ZIPKIN_PORT=9411
