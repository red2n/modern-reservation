server:
  port: 8080

spring:
  application:
    name: gateway-service
  config:
    import: "configserver:http://localhost:8888/config"
  cloud:
    config:
      username: config-admin
      password: ${CONFIG_SERVER_PASSWORD:admin123}
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        # Reservation Engine Routes
        - id: reservation-engine
          uri: lb://reservation-engine
          predicates:
            - Path=/api/v1/reservations/**
          filters:
            - name: CircuitBreaker
              args:
                name: reservation-circuit-breaker
                fallbackUri: forward:/fallback/reservation
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenish-rate: 10
                redis-rate-limiter.burst-capacity: 20
                redis-rate-limiter.requested-tokens: 1

        # Availability Calculator Routes
        - id: availability-calculator
          uri: lb://AVAILABILITY-CALCULATOR
          predicates:
            - Path=/api/v1/availability/**
          filters:
            - name: CircuitBreaker
              args:
                name: availability-circuit-breaker
                fallbackUri: forward:/fallback/availability
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenish-rate: 20
                redis-rate-limiter.burst-capacity: 40

        # Payment Processor Routes
        - id: payment-processor
          uri: lb://PAYMENT-PROCESSOR-SERVICE
          predicates:
            - Path=/api/v1/payments/**
          filters:
            - name: CircuitBreaker
              args:
                name: payment-circuit-breaker
                fallbackUri: forward:/fallback/payment
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenish-rate: 5
                redis-rate-limiter.burst-capacity: 10

        # Rate Management Routes
        - id: rate-management
          uri: lb://rate-management
          predicates:
            - Path=/api/v1/rates/**
          filters:
            - name: CircuitBreaker
              args:
                name: rate-circuit-breaker
                fallbackUri: forward:/fallback/rate

        # Analytics Engine Routes
        - id: analytics-engine
          uri: lb://analytics-engine
          predicates:
            - Path=/api/v1/analytics/**
          filters:
            - name: CircuitBreaker
              args:
                name: analytics-circuit-breaker
                fallbackUri: forward:/fallback/analytics

        # Batch Processor Routes
        - id: batch-processor
          uri: lb://batch-processor
          predicates:
            - Path=/api/v1/batch/**
          filters:
            - name: CircuitBreaker
              args:
                name: batch-circuit-breaker
                fallbackUri: forward:/fallback/batch

        # Service-Prefixed API Routes (for Swagger UI integration)
        - id: payment-processor-api
          uri: lb://PAYMENT-PROCESSOR-SERVICE
          order: -1
          predicates:
            - Path=/payment-processor/api/**
          filters:
            - RewritePath=/payment-processor/api/(?<path>.*), /api/$\{path}

        - id: reservation-engine-api
          uri: lb://reservation-engine
          order: -1
          predicates:
            - Path=/reservation-engine/api/**
          filters:
            - RewritePath=/reservation-engine/api/(?<path>.*), /api/$\{path}

        - id: analytics-engine-api
          uri: lb://analytics-engine
          order: -1
          predicates:
            - Path=/analytics-engine/api/**
          filters:
            - RewritePath=/analytics-engine/api/(?<path>.*), /api/$\{path}

        - id: availability-calculator-api
          uri: lb://AVAILABILITY-CALCULATOR
          order: -1
          predicates:
            - Path=/availability-calculator/api/**
          filters:
            - RewritePath=/availability-calculator/api/(?<path>.*), /api/$\{path}

        - id: rate-management-api
          uri: lb://rate-management
          order: -1
          predicates:
            - Path=/rate-management/api/**
          filters:
            - RewritePath=/rate-management/api/(?<path>.*), /api/$\{path}

        # Node.js Services Routes
        - id: api-gateway-node
          uri: lb://api-gateway-node
          predicates:
            - Path=/api/v1/graphql/**

        - id: notification-service
          uri: lb://notification-service
          predicates:
            - Path=/api/v1/notifications/**

        - id: websocket-service
          uri: lb://websocket-service
          predicates:
            - Path=/ws/**

        # Swagger UI Documentation Routes for Business Services
        - id: reservation-engine-docs
          uri: lb://reservation-engine
          order: -1
          predicates:
            - Path=/reservation-engine/api-docs/**
          filters:
            - RewritePath=/reservation-engine/api-docs/(?<path>.*), /api-docs/$\{path}

        - id: availability-calculator-docs
          uri: lb://AVAILABILITY-CALCULATOR
          order: -1
          predicates:
            - Path=/availability-calculator/api-docs/**
          filters:
            - RewritePath=/availability-calculator/api-docs/(?<path>.*), /api-docs/$\{path}

        - id: payment-processor-docs
          uri: lb://PAYMENT-PROCESSOR-SERVICE
          order: -1
          predicates:
            - Path=/payment-processor/api-docs/**
          filters:
            - RewritePath=/payment-processor/api-docs/(?<path>.*), /api-docs/$\{path}

        - id: rate-management-docs
          uri: lb://RATE-MANAGEMENT
          order: -1
          predicates:
            - Path=/rate-management/api-docs/**
          filters:
            - RewritePath=/rate-management/api-docs/(?<path>.*), /api-docs/$\{path}

        - id: analytics-engine-docs
          uri: lb://analytics-engine
          order: -1
          predicates:
            - Path=/analytics-engine/api-docs/**
          filters:
            - RewritePath=/analytics-engine/api-docs/(?<path>.*), /api-docs/$\{path}

        # Swagger UI Routes for Business Services
        - id: availability-calculator-swagger-ui
          uri: lb://AVAILABILITY-CALCULATOR
          order: -1
          predicates:
            - Path=/availability-calculator/swagger-ui/**
          filters:
            - RewritePath=/availability-calculator/swagger-ui/(?<path>.*), /availability-calculator/swagger-ui/$\{path}

        - id: payment-processor-swagger-ui
          uri: lb://PAYMENT-PROCESSOR-SERVICE
          order: -1
          predicates:
            - Path=/payment-processor/swagger-ui/**
          filters:
            - RewritePath=/payment-processor/swagger-ui/(?<path>.*), /payment-processor/swagger-ui/$\{path}

        - id: rate-management-swagger-ui
          uri: lb://RATE-MANAGEMENT
          order: -1
          predicates:
            - Path=/rate-management/swagger-ui/**
          filters:
            - RewritePath=/rate-management/swagger-ui/(?<path>.*), /rate-management/swagger-ui/$\{path}

        - id: analytics-engine-swagger-ui
          uri: lb://ANALYTICS-ENGINE
          order: -1
          predicates:
            - Path=/analytics-engine/swagger-ui/**
          filters:
            - RewritePath=/analytics-engine/swagger-ui/(?<path>.*), /analytics-engine/swagger-ui/$\{path}

        - id: reservation-engine-swagger-ui
          uri: lb://RESERVATION-ENGINE
          order: -1
          predicates:
            - Path=/reservation-engine/swagger-ui/**
          filters:
            - RewritePath=/reservation-engine/swagger-ui/(?<path>.*), /swagger-ui/$\{path}

  # Redis Configuration for Rate Limiting
  redis:
    host: localhost
    port: 6379
    timeout: 2000ms

  # Main Configuration
  main:
    allow-bean-definition-overriding: true
    web-application-type: reactive

# Circuit Breaker Configuration
resilience4j:
  circuitbreaker:
    instances:
      reservation-circuit-breaker:
        failure-rate-threshold: 50
        wait-duration-in-open-state: 10000
        sliding-window-size: 10
        minimum-number-of-calls: 5
      availability-circuit-breaker:
        failure-rate-threshold: 50
        wait-duration-in-open-state: 10000
        sliding-window-size: 10
        minimum-number-of-calls: 5
      payment-circuit-breaker:
        failure-rate-threshold: 30
        wait-duration-in-open-state: 15000
        sliding-window-size: 10
        minimum-number-of-calls: 3

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:modernReservationSecretKey2024}
  expiration: 86400000 # 24 hours

# Management and Monitoring
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,gateway,circuitbreakers
  endpoint:
    health:
      show-details: always

# Logging Configuration
logging:
  level:
    com.modernreservation: DEBUG
    org.springframework.cloud.gateway: DEBUG
    org.springframework.security: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%X{traceId:-},%X{spanId:-}] [%thread] %-5level [%logger{36}] - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%X{traceId:-},%X{spanId:-}] [%thread] %-5level [%logger{36}] - %msg%n"
  file:
    name: ../../logs/infrastructure/gateway-service.log

# Application Information
info:
  app:
    name: Modern Reservation Gateway Service
    description: API Gateway with routing, load balancing, and security
    version: 2.0.0

# SpringDoc OpenAPI Configuration for Gateway
springdoc:
  api-docs:
    enabled: true
    path: /api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    config-url: /api-docs/swagger-config
    urls:
      - url: /api-docs
        name: Gateway Service
      - url: /reservation-engine/api-docs
        name: 🏨 Reservation Engine
      - url: /availability-calculator/api-docs
        name: 📅 Availability Calculator
      - url: /payment-processor/api-docs
        name: 💳 Payment Processor
      - url: /rate-management/api-docs
        name: 💰 Rate Management
      - url: /analytics-engine/api-docs
        name: 📊 Analytics Engine
    urls-primary-name: Gateway Service
    display-request-duration: true
    groups-order: ASC
    operations-sorter: alpha
    tags-sorter: alpha
    disable-swagger-default-url: false
    show-extensions: true
    show-common-extensions: true
    deep-linking: true
    display-operation-id: false
    default-models-expand-depth: 1
    default-model-expand-depth: 1
    default-model-rendering: example
    doc-expansion: none
    filter: false
    oauth2-redirect-url: http://localhost:8080/swagger-ui/oauth2-redirect.html
  show-actuator: true
